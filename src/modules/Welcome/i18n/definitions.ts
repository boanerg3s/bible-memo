declare namespace App {
  export interface Locale {
    welcome: {
      pages: {
        presentation: {
          title: string;
          description: string;
          action: string;
        };
      };
    };
  }
}
